controller_manager:
  ros__parameters:
    update_rate: 50
    use_sim_time: true

    joint_state_broadcaster:
      type: joint_state_broadcaster/JointStateBroadcaster

    diff_drive_controller:
      type: diff_drive_controller/DiffDriveController

diff_drive_controller:
  ros__parameters:
    left_wheel_names: ["drivewhl_l1_joint", "drivewhl_l2_joint", "drivewhl_l3_joint"]
    right_wheel_names: ["drivewhl_r1_joint", "drivewhl_r2_joint", "drivewhl_r3_joint"]
    wheel_separation: 0.4
    wheel_radius: 0.1
    publish_rate: 50.0
    use_stamped_vel: false
    base_frame_id: base_link
    odom_frame_id: odom
    enable_odom_tf: true
